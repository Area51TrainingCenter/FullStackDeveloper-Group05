import { HoverAvanzadoDirective } from './hover-avanzado.directive';

describe('HoverAvanzadoDirective', () => {
  it('should create an instance', () => {
    const directive = new HoverAvanzadoDirective();
    expect(directive).toBeTruthy();
  });
});
