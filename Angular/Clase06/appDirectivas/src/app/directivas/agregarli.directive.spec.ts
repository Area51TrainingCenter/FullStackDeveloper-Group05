import { AgregarliDirective } from './agregarli.directive';

describe('AgregarliDirective', () => {
  it('should create an instance', () => {
    const directive = new AgregarliDirective();
    expect(directive).toBeTruthy();
  });
});
