import { ProcesandoDirective } from './procesando.directive';

describe('ProcesandoDirective', () => {
  it('should create an instance', () => {
    const directive = new ProcesandoDirective();
    expect(directive).toBeTruthy();
  });
});
