import { FiltrarNovelasPipe } from './filtrar-novelas.pipe';

describe('FiltrarNovelasPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltrarNovelasPipe();
    expect(pipe).toBeTruthy();
  });
});
