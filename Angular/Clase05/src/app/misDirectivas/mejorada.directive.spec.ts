import { MejoradaDirective } from './mejorada.directive';

describe('MejoradaDirective', () => {
  it('should create an instance', () => {
    const directive = new MejoradaDirective();
    expect(directive).toBeTruthy();
  });
});
